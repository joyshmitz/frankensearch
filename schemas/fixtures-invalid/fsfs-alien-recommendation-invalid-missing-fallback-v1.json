{
  "kind": "fsfs_alien_recommendation_card",
  "v": 1,
  "subsystem": "degradation_scheduler",
  "ev_score": 3.7,
  "priority_tier": "A",
  "adoption_wedge": "Deploy on pressure-heavy hosts first.",
  "budgeted_mode": {
    "latency_budget_ms": 20,
    "memory_budget_mb": 64,
    "retry_budget": 0,
    "on_exhaustion": "Hold degraded mode until pressure recovers."
  },
  "baseline_comparator": "Static threshold-only degradation logic.",
  "isomorphism_proof_plan": {
    "invariants": [
      "Transitions remain deterministic for identical telemetry input."
    ],
    "baseline_harness": "tests/e2e_degrade_replay.sh",
    "replay_checks": [
      "transition_trace_equivalence"
    ]
  },
  "repro_artifacts": {
    "manifest_fields": [
      "seed",
      "config_hash",
      "subsystem"
    ],
    "artifact_outputs": [
      "degrade_trace.jsonl",
      "degrade_summary.json"
    ],
    "replay_command": "cargo test -p frankensearch --test degrade_replay"
  },
  "rollback_plan": {
    "rollback_command": "fsfsctl policy set degrade.static=true",
    "abort_conditions": [
      "state_oscillation_gt_3_per_hour"
    ]
  }
}
